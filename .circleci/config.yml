version: 2.1
orbs:
  cypress: cypress-io/cypress@3
workflows:
  test:
    jobs:
      - cypress/run:
          install-browsers: true
          start-command: 'cd client && npm install && npm run start'
          wait-on: 'http://localhost:3000'
          cypress-command: 'npx cypress run --headless'